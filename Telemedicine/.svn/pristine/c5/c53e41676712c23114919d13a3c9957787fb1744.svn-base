import { TestBed } from '@angular/core/testing';

import { TelemedicineInterceptor } from './telemedicine.interceptor';

describe('TelemedicineInterceptor', () => {
  beforeEach(() => TestBed.configureTestingModule({
    providers: [
      TelemedicineInterceptor
      ]
  }));

  it('should be created', () => {
    const interceptor: TelemedicineInterceptor = TestBed.inject(TelemedicineInterceptor);
    expect(interceptor).toBeTruthy();
  });
});
