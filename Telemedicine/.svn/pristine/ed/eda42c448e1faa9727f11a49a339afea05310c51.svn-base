import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PatienAutherComponent } from './patien-auther.component';

describe('PatienAutherComponent', () => {
  let component: PatienAutherComponent;
  let fixture: ComponentFixture<PatienAutherComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PatienAutherComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PatienAutherComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
