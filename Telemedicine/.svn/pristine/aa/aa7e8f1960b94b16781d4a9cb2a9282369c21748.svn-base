import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ScribeStatusComponent } from './scribe-status.component';

describe('ScribeStatusComponent', () => {
  let component: ScribeStatusComponent;
  let fixture: ComponentFixture<ScribeStatusComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ScribeStatusComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ScribeStatusComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
