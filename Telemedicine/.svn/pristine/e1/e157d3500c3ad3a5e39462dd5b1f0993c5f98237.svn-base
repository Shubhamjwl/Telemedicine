import { ComponentFixture, TestBed } from '@angular/core/testing';

import { VerfymobileprofileComponent } from './verfymobileprofile.component';

describe('VerfymobileprofileComponent', () => {
  let component: VerfymobileprofileComponent;
  let fixture: ComponentFixture<VerfymobileprofileComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ VerfymobileprofileComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(VerfymobileprofileComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
